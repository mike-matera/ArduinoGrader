#include <iostream>

#include "emulator.h"
/*
#include "freq.h" 

using std::cout; 
using std::endl;
using std::string; 

FrequencyCounter f; 

void test_start(void) {
  emu.reg_callback({13}, f);
}

void test_setup(void) {
}

bool test_loop(int count) {
  return (count < 1);
}

void test_exit(void) {
  unsigned long ont = f.getOnTime(); 
  unsigned long offt = f.getOffTime(); 

  cout << "Period: " << f.getPeriod() << " high: " << ont << " low: " << offt << endl; 

  if ((ont / 1000) == 200 && (offt / 1000) == 400) {
    cout << "*** TEST PASSED ***" << endl;
  }else{
    cout << "*** TEST FAILED ***" << endl;
  }
  
}

void test_check(const std::string &what) {
  if (emu.get_time() > 5000000) {
    throw std::string("Simulator forced to exit after 5 seconds.");
  }
}
*/
